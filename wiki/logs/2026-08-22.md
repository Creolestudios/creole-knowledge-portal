# 2026-08-22 — Greenfield redeploy (cloud_user account 715736407442)

## Summary

Redeployed CKP infrastructure and web app to the **current `cloud_user` AWS account** (`715736407442`), after local `main` was reset to rewritten `origin/main` (`e0db308`).

## Completed

| Item | Detail |
|------|--------|
| Git | `git reset --hard origin/main` |
| Pulumi backend | `s3://pulumi-state-715736407442` + KMS `alias/ckp-pulumi-secrets` |
| Pulumi stack `dev` | 40 resources created (~12m) |
| ECS | Cluster `ckp-shared`; web desired=1 |
| ALB | `ckp-shared-alb-889679340.us-east-1.elb.amazonaws.com` |
| HTTPS / ACM | Cert **ISSUED** for `ckp.nikcreations.com` |
| Route53 | Zone `Z0723044OGD1D3RWLH2A` — **NS delegated** (public DNS matches all 4 AWS NS) |
| Web ECR image | `creole-knowledge-portal-web:latest` pushed |
| Web live | **HTTPS 200** at custom domain + ALB |

## Route53 NS (delegate `ckp` under nikcreations.com)

- `ns-1141.awsdns-14.org`
- `ns-1912.awsdns-47.co.uk`
- `ns-228.awsdns-28.com`
- `ns-588.awsdns-09.net`

## Fixes during deploy

- **Docker build OOM:** added `NODE_OPTIONS=--max-old-space-size=4096` to root `Dockerfile` `npm run build` step.
- **zsh ECR tag:** use `${ECR}:latest` not `$ECR:latest` (`:l` is a zsh modifier).

## Repo changes (branch `deploy/infra-sync-715736407442`, commit `99b8d64`)

**Merged to `main`** (fast-forward `e0db308` → `6c2524f`, pushed to `origin/main` ~14:10 IST). Originally on branch `deploy/infra-sync-715736407442`:

| File | Change |
|------|--------|
| `README.md` | **GitHub CI/CD Setup** section — full pending checklist (OIDC variable, Pulumi backend, Supabase redirects, API/worker scale-up, no-secrets-in-git warning) |
| `Dockerfile` | `NODE_OPTIONS=--max-old-space-size=4096` on the `npm run build` `RUN` step |
| `infra/Pulumi.yaml` | Pulumi backend URL → `s3://pulumi-state-715736407442` |
| `infra/Pulumi.dev.yaml` | Stack config for account `715736407442`: new KMS `encryptedkey`, `ckp:albSubnetIds`, `ckp:githubRepo`; removed hardcoded `ckp:webImage` pointing at old-account ECR (`761341389675`) |

Backup of the prior dev config: `infra/Pulumi.dev.yaml.old-account.bak` — **gitignored**, kept local only.

## Architecture notes

- **External data stores only:** Postgres/auth via Supabase, Mongo via Atlas, Redis via external broker — no RDS, DocumentDB, or ElastiCache in AWS (`infra/Pulumi.yaml` description: "data stores external").
- **Web-only scale:** `webDesiredCount=1`; `apiDesiredCount=0`, `workersDesiredCount=0`.

## Documentation sync (wiki + repo)

- **Wiki** ([Sync wiki to git changes](03dd9913-7f74-44e4-be2b-b3b2b16c7d64)): updated `wiki/pages/architecture.md`, `wiki/logs/2026-08-22.md`, supersession note on `2026-08-11.md`.
- **Follow-up:** synced repo docs to account `715736407442` — `infra/README.md`, `infra/PULUMI-BACKEND.md`, root `README.md`, `.github/OIDC-SETUP.md`, `.github/workflows/deploy-infra.yml`.
- **Still stale in `infra/README.md`:** old ALB hostname (`ckp-shared-alb-495275305`), old Route53 zone ID, “imported CLI bootstrap” wording (greenfield stack in new account).



## Fetch-blogs / pipeline secrets (dev stack)

| Item | Detail |
|------|--------|
| `ckp:geminiApiKey` (web) | Set (AIza…); unchanged — used by Next.js ECS web task as `GEMINI_API_KEY` |
| `ckp:llmGeminiApiKey` (fetch-blogs) | **Updated** to dedicated LLM key (AQ… prefix); deployed to Secrets Manager `…/llm-gemini-api-key` (`pulumi up` ~18s, secret version replaced) |
| `ckp:mongoUri` | **Not set** — user local `mongodb://localhost:27017` was **not** applied (Fargate cannot use localhost) |
| `ckp:redisUrl` | **Already set** to external Redis (not localhost); local `redis://localhost:6379/*` was **not** applied |
| `MONGO_DB_NAME` | Not a Pulumi key; fetch-blogs uses app default unless set in task env (not wired in `infra/index.ts` today) |
| `LLM_GEMINI_MODEL` / other `APP_*` | Not set in Pulumi; API/worker tasks only get `APP_ENVIRONMENT=production` plus Mongo/Redis/Celery env — model stays fetch-blogs code defaults |

**Next for API/workers:** provide Atlas `ckp:mongoUri` (after auth/network fixed); confirm existing external Redis is the intended broker; build/push API + workers images; set `apiDesiredCount` / `workersDesiredCount` > 0 (do not scale with placeholder Mongo).

## Pulumi Supabase secrets (dev stack)

| Item | Detail |
|------|--------|
| Backend | `s3://pulumi-state-715736407442` (`AWS_PROFILE=cloud_user`) |
| Stack config (encrypted) | `ckp:supabaseUrl`, `ckp:supabaseAnonKey`, `ckp:supabaseServiceRoleKey` — set via `pulumi config set --secret` (values not logged here) |
| Skipped (no real values) | `ckp:mongoUri` only — Supabase, Redis, and Gemini keys are set in stack + Secrets Manager |
| `pulumi up` | **Success** (~46s): 6 Secrets Manager resources created, ECS web task definition replaced, web service updated |
| AWS Secrets Manager | `creole-knowledge-portal/dev/supabase-url`, `supabase-anon-key`, `supabase-service-role-key` |
| Web image | Rebuilt with real `NEXT_PUBLIC_SUPABASE_*` build-args; pushed `715736407442.dkr.ecr.us-east-1.amazonaws.com/creole-knowledge-portal-web:latest`; ECS force redeploy on `creole-knowledge-portal-web-service-5b5569d` |
| Web task def | Rev **3** — secrets: `NEXT_PUBLIC_SUPABASE_*`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY` |
| Live URL (post-redeploy) | **HTTPS 200** at `https://ckp.nikcreations.com/creole-knowledge-portal/` |

### Supabase dashboard redirect URLs (manual — required for login)

In **Supabase → Authentication → URL Configuration**:

| Setting | Value |
|---------|-------|
| **Site URL** | `https://ckp.nikcreations.com/creole-knowledge-portal` |
| **Redirect URLs** | `https://ckp.nikcreations.com/creole-knowledge-portal/auth/callback` |
| | `http://ckp-shared-alb-889679340.us-east-1.elb.amazonaws.com/creole-knowledge-portal/auth/callback` |
| | `http://localhost:3000/auth/callback` |

**Why web image rebuild:** Next.js inlines `NEXT_PUBLIC_*` at Docker **build time** into the client bundle; ECS runtime secrets alone do not fix browser-side Supabase init.

## MongoDB Atlas verification

| Attempt | User | Result |
|---------|------|--------|
| `creole@123` (URI-encoded `%40`) | `priyadhanani_db_user` @ `cluster0.aqmcunr.mongodb.net` | **FAIL** — `bad auth : authentication failed` (Atlas reached; credentials rejected) |
| Updated password (URL-encoded `%` → `%25` in URI) | `priyadhanani_db_user` @ `cluster0.aqmcunr.mongodb.net` / `knowledge_portal` | **FAIL** — `bad auth : authentication failed` via `pymongo` (`uv run` in `fetch-blogs`): full SRV URI, `quote_plus`, separate username/password, `authSource` `admin` / `knowledge_portal` |

**Not applied:** `ckp:mongoUri` remains unset in Pulumi stack `dev`. Retry after resetting the DB user password in Atlas UI (enter password literally; only URL-encode in the connection string). For ECS, allow **0.0.0.0/0** (or AWS egress IPs) in Atlas Network Access.

**2026-08-22 retest:** Password `4G-TV_%7ghugtX2` (URI-encoded `%` → `%25`) **works** via pymongo ping. **`ckp:mongoUri` set in Pulumi** + `pulumi up` (~34s) → SM `creole-knowledge-portal/dev/mongo-uri`. Earlier password `creole@123` failed auth.


## Redis (external broker)

| Item | Detail |
|------|--------|
| Connection test | **SUCCESS** — `redis-cli PING` → `PONG` against `cars-smell-terracottaish-35722.db.redis.io:19445` (password redacted) |
| Pulumi stack `dev` | `ckp:redisUrl` set via `pulumi config set --secret` (encrypted in stack config; not committed) |
| AWS Secrets Manager | `creole-knowledge-portal/dev/redis-url` |
| ECS env mapping | Same secret injected as `REDIS_URL`, `REDIS_RESULT_URL`, and `CELERY_BROKER_URL` (`infra/index.ts`) |
| `pulumi up` | **Success** (~23s): stack already matched desired state after parallel deploy (50 unchanged) |

## Pulumi Gemini secrets (dev stack)

| Item | Detail |
|------|--------|
| Stack config | `ckp:geminiApiKey`, `ckp:llmGeminiApiKey` — set by parallel subagent run (~13:11 IST); values not logged here |
| AWS Secrets Manager | `creole-knowledge-portal/dev/gemini-api-key`, `creole-knowledge-portal/dev/llm-gemini-api-key` |
| Note | Applied during the same locked `pulumi up` that created `redis-url` |
| Web rollout fix | After task def `:3` hit `CannotPullContainerError` (missing `linux/amd64` manifest), web image re-pushed with `--platform linux/amd64` and ECS force redeploy |

## Not done

- **Supabase redirect URLs** in dashboard (see table above) — blocks magic link / OAuth callback
- API / workers images and scale-up (`apiDesiredCount=0`, `workersDesiredCount=0`; ECR api/workers repos **empty**)
- **MongoDB Atlas:** `ckp:mongoUri` **set** in Pulumi + SM (working password confirmed 2026-08-22)
- GitHub variable `AWS_GHA_DEPLOY_ROLE_ARN` → `arn:aws:iam::715736407442:role/ckp-github-deploy-dev`
- Commit and push infra + Dockerfile changes to `main` (stack secrets live in encrypted Pulumi config only)

## URLs

- **Primary (use now):** https://ckp.nikcreations.com/creole-knowledge-portal/
- **ALB fallback:** https://ckp-shared-alb-889679340.us-east-1.elb.amazonaws.com/creole-knowledge-portal/
- **Apex** `https://ckp.nikcreations.com/` → 404 (expected; app is path-prefixed)

## CKP deploy access test (2026-08-22 ~13:20 IST)

Account **715736407442** (`cloud_user`). Pulumi stack **dev** (`infra/`). No credential values logged.

| Access | Config in Pulumi? | Connection test | Status | Notes |
|--------|-------------------|-----------------|--------|-------|
| MongoDB Atlas | `ckp:mongoUri` **not in file** | `pymongo` admin ping (`fetch-blogs` / uv) | **WORKING** | Cluster `cluster0.aqmcunr.mongodb.net`, db `knowledge_portal`; alternate password candidate failed (timeout); working credential not stored in Pulumi yet |
| Redis | `ckp:redisUrl` **encrypted** | PONG (URL from SM `creole-knowledge-portal/dev/redis-url`) | **WORKING** | Host `cars-smell-terracottaish-35722.db.redis.io:19445` |
| Supabase | `ckp:supabaseUrl`, anon/service keys **encrypted** | `/auth/v1/health` → 200; `/rest/v1/` → 401 | **WORKING** | Project reachable; REST root 401 is expected without a table route |
| Gemini (web) | `ckp:geminiApiKey` **encrypted** | Google `v1/models` → 200 (key from SM) | **WORKING** | ECS web task injects `GEMINI_API_KEY` from SM |
| Gemini LLM (fetch-blogs) | `ckp:llmGeminiApiKey` **encrypted** | Google `v1/models` → 200 (key from SM) | **WORKING** | API/workers task defs reference `LLM_GEMINI_API_KEY` (services scaled to 0) |
| Live app (web) | n/a | `curl` → 308 → **200** | **WORKING** | https://ckp.nikcreations.com/creole-knowledge-portal/ |
| Live API health | n/a | `curl` `/api/v1/health` → **503** | **NOT WORKING** | `ckp:apiDesiredCount=0`; no running API tasks |
| ECS — web | n/a | `describe-services` | **ACTIVE** | desired **1**, running **1**, pending **1** (deployment in progress) |
| ECS — api | n/a | `describe-services` | **ACTIVE (scaled down)** | desired **0**, running **0** |
| ECS — workers | n/a | `describe-services` | **ACTIVE (scaled down)** | desired **0**, running **0** |
| AWS CLI / ECS | n/a | `sts get-caller-identity`, cluster `ckp-shared` | **WORKING** | |
| Pulumi CLI config read | n/a | `pulumi config get` | **BLOCKED locally** | S3 state backend credential refresh failed; use `Pulumi.dev.yaml` + SM for secret presence |

### Pulumi `ckp:*` summary (names only)

**Set (plaintext):** `environment`, `appName`, `domainName`, `listenerPriorityBase`, `ecsDesiredCount`, `webDesiredCount`, `apiDesiredCount`, `workersDesiredCount`, `ecsCpu`, `ecsMemory`, `githubRepo`, `albSubnetIds`.

**Set (encrypted):** `redisUrl`, `supabaseUrl`, `supabaseAnonKey`, `supabaseServiceRoleKey`, `geminiApiKey`, `llmGeminiApiKey`.

**Missing / not in stack file:** `mongoUri`, `mongoPassword`, `redisPassword` (Redis uses full URL in `redisUrl`).

**AWS Secrets Manager (dev):** `redis-url`, `supabase-url`, `supabase-anon-key`, `supabase-service-role-key`, `gemini-api-key`, `llm-gemini-api-key` — all present.

### Fix next

1. Set **`ckp:mongoUri`** (secret) in Pulumi and wire Mongo for API/workers when scaling them up.
2. Scale **API/workers** (`apiDesiredCount`, `workersDesiredCount`) and ensure ECR images exist so `/api/v1/health` is not 503.
3. Complete **Supabase redirect URLs** in dashboard (still required for auth callbacks).
4. Fix local **Pulumi backend AWS credentials** if you need `pulumi config get` against remote state (stack file still has encrypted values).


## API CloudWatch / auth log check (~13:30 IST)

Investigation: ECS + log group `creole-knowledge-portal-logs-ebf7cc5` (account `715736407442`, profile `cloud_user`).

| Check | Result |
|-------|--------|
| ECS `creole-knowledge-portal-api-service-d2c8796` | **ACTIVE**, desired **0**, running **0**, pending **0** |
| Recent ECS events | Steady state / deployment completed (~13:26 IST); **no** task failures, image pull errors, or crash loops in event history |
| Stopped API tasks | None listed |
| ALB TG `ckp-dev-api` | **No registered targets** (empty health list) |
| Health URL | `https://ckp.nikcreations.com/creole-knowledge-portal/api/v1/health` → **503** (~3.5s) |
| API ECR `creole-knowledge-portal-api` | **0 images** (nothing to pull if scaled up without a push) |
| API CloudWatch streams | **None** (`awslogs-stream-prefix: api`); only `web/web/*` streams exist |
| Auth keyword search (48h, whole log group) | **No** matches for auth, 401, 403, unauthorized, JWT, token, supabase, forbidden, ECONNREFUSED in API logs (API never emitted logs) |

**Web log excerpts (not API):**

- Repeated: `[Middleware] Path: /, User: none, Admin: false` (ALB health checks / anonymous `/` — not an error).
- Historical (older web task): `API key should be set when using the Gemini API.` (client-side Gemini init before/with missing build-time key; later redeploys inject `GEMINI_API_KEY` from SM).

**Auth-related context elsewhere (not in API container logs):**

- Mongo Atlas `bad auth : authentication failed` was observed during credential testing (see MongoDB section above); `ckp:mongoUri` later set after a working password was confirmed.
- Supabase `/rest/v1/` returning **401** without a JWT is expected; login still needs dashboard redirect URLs configured.

**Likely root cause of “API auth errors” / 503:** Backend API is **intentionally scaled to zero** (`ckp:apiDesiredCount=0` in `Pulumi.dev.yaml`). ALB has no healthy API targets, so health and all `/api/*` routes return **503**. There is **no evidence of runtime API auth failures in CloudWatch** because the API task has not run (no `api/*` log streams).

**Fix:**

1. Build and push `creole-knowledge-portal-api:latest` to ECR (`linux/amd64`).
2. Confirm Secrets Manager has `mongo-uri`, `redis-url`, Supabase, and LLM Gemini keys for API task def.
3. Set `ckp:apiDesiredCount` to `1` and `pulumi up` (or update service desired count); verify TG registers healthy target and health URL returns **200**.
4. Re-run log filter on `api/*` streams after scale-up for real auth/JWT/Mongo errors.
5. Complete Supabase redirect URLs for browser auth (separate from API availability).

## README — GitHub CI/CD checklist (~13:50 IST)

Added **GitHub CI/CD Setup** to root `README.md` (8 subsections, actionable checkboxes). Consolidates all pending team steps:

- GitHub variable `AWS_GHA_DEPLOY_ROLE_ARN` (still pending in repo settings)
- OIDC role already in AWS via Pulumi — reference only
- Pulumi backend URL for CI + local
- Secrets placement (Pulumi/SM, **not** GitHub Actions)
- Supabase Site URL + callback URLs (pending dashboard config)
- API/workers image build + ECR push + `apiDesiredCount` / `workersDesiredCount` scale-up
- Health verification targets
- Team resync after `main` history rewrite

Secrets subsection under **Secrets** now links to this section instead of duplicating the variable table. Documented in commit `99b8d64` on branch `deploy/infra-sync-715736407442` (same PR as infra sync).

## deploy-app.yml workflow (~14:18 IST)

Added [`.github/workflows/deploy-app.yml`](../.github/workflows/deploy-app.yml) — builds `fetch-blogs/` on **ubuntu-latest** (native amd64), pushes to ECR `creole-knowledge-portal-api` + `creole-knowledge-portal-workers`, optional Pulumi scale-up via manual workflow_dispatch. Requires `AWS_GHA_DEPLOY_ROLE_ARN` (same OIDC role as infra deploy).

## Production URL fixes (~14:24 IST)

- **Web ECS:** `BLOG_SERVICE_URL` → `https://ckp.nikcreations.com/creole-knowledge-portal/api/v1` (was unset; server routes defaulted to `localhost:8000`).
- **API ECS:** `APP_CORS_ORIGINS` → `https://ckp.nikcreations.com,http://localhost:3000` (API CORS was localhost-only in code).
- **Code:** `fetch-blogs/src/core/config.py` + `main.py` read `APP_CORS_ORIGINS`; requires `pulumi up` + new API image for CORS change to take effect on running tasks.

