# 2026-08-11 — Deploy-readiness fixes (feat/infra)

## Summary

Batch of fixes to unblock CI quality gate and prepare first ECS deploy with external data stores (Atlas + Upstash) and Supabase auth.

## Build & TypeScript

- Fixed dashboard TypeScript errors: optional `user`/`profile` props on `DailyBlogTab` and `ActivityTab`; removed invalid `PastBlogsTab` props from `DashboardShell`.
- Added `basePath` / `assetPrefix` via `NEXT_PUBLIC_BASE_PATH` in `next.config.ts` (production: `/creole-knowledge-portal`).
- Updated `Dockerfile` build-args: `NEXT_PUBLIC_BASE_PATH`, `NEXT_PUBLIC_SUPABASE_*`, server placeholders for CI build.

## Infra & secrets

- Added [`infra/components/app-secrets.ts`](../infra/components/app-secrets.ts) — Pulumi config → AWS Secrets Manager → ECS task `secrets`.
- Wired ECS env for web (Supabase, Gemini), api/workers (Mongo, Redis, `LLM_GEMINI_API_KEY`).
- Execution role IAM policy for `secretsmanager:GetSecretValue`.
- Web target group health check path → `/creole-knowledge-portal`.
- `NEXT_PUBLIC_API_URL` corrected (no duplicate `/api` suffix).
- `Pulumi.example.yaml`: `apiDesiredCount: 1` with dev override guidance.

## Security & CI

- Mock-user auth bypass gated behind `NODE_ENV !== 'production'` (`middleware.ts`, API routes, `lib/dev/mock-user.ts`).
- ESLint / Vitest ignore `infra/**` and `fetch-blogs/**`.

## Tests

- fetch-blogs: Beanie init autouse fixture in `tests/conftest.py` — all 15 pytest tests pass.
- Next.js: `npm run build` passes with CI placeholder env; Vitest 37/37 pass; infra `tsc --noEmit` pass.

## Documentation

- Rewrote root [`README.md`](../README.md) and [`infra/README.md`](../infra/README.md) to match wired secrets, basePath, Docker build-args, scaling guidance, and Supabase redirect URLs.

## Remaining blockers for live deploy

1. Set real Pulumi secrets: `mongoUri`, `redisUrl`, Supabase keys, Gemini keys.
2. Build web image with production `NEXT_PUBLIC_*` build-args; push to ECR.
3. Atlas IP allowlist for VPC NAT egress; Upstash Redis URL.
4. Supabase redirect URLs with `/creole-knowledge-portal/auth/callback`.
5. Route53 NS delegation at `nikcreations.com`.
6. GitHub variable `AWS_GHA_DEPLOY_ROLE_ARN` for deploy workflow.
7. API ALB path prefix: FastAPI receives full path `/creole-knowledge-portal/api/...` — may need strip middleware if external routing fails (health check via TG is OK).

---

## Pulumi deploy executed (2026-08-11 afternoon)

**Stack:** `dev` · **Account:** `761341389675` · **Profile:** `cloud_user`

### Result: **PARTIAL** — web container live on ALB; auth and custom domain pending

| Item | Status |
|------|--------|
| Pulumi `pulumi up` (23 resources) | ✅ |
| Route53 `ckp.nikcreations.com` | ✅ zone created — NS delegation pending |
| ECR + web image push | ✅ `creole-knowledge-portal-web:latest` |
| ECS web service | ✅ 1 task running, target healthy |
| ALB HTTP 200 | ✅ `/creole-knowledge-portal/` |
| GitHub OIDC provider | ✅ Created |
| IAM role `ckp-github-deploy-dev` | ✅ Created with inline deploy policy |
| API / workers | ⏸ desiredCount=0 |
| Supabase login | ⏸ placeholder env in container |

### Route53 nameservers (Cloudflare delegation for `ckp`)

1. `ns-1277.awsdns-31.org`
2. `ns-1670.awsdns-16.co.uk`
3. `ns-423.awsdns-52.com`
4. `ns-525.awsdns-01.net`

### URLs

- **ALB (test now):** http://ckp-shared-alb-495275305.us-east-1.elb.amazonaws.com/creole-knowledge-portal/
- **Custom domain (HTTP, DNS live):** http://ckp.nikcreations.com/creole-knowledge-portal/
- **HTTPS:** ACM + ALB :443 (no CloudFront) — added in Pulumi; HTTP on custom domain 301s to HTTPS

### README accuracy (same day)

Root [`README.md`](../README.md) and [`infra/README.md`](../infra/README.md) now document: path-based URLs (no `api.` subdomain), HTTP vs HTTPS, NS records, and API still scaled to 0.

### GitHub variable to set

| Variable | Value |
|----------|-------|
| `AWS_GHA_DEPLOY_ROLE_ARN` | `arn:aws:iam::761341389675:role/ckp-github-deploy-dev` |

### Supabase redirect URLs

- Site URL: `https://ckp.nikcreations.com/creole-knowledge-portal`
- Redirect: `https://ckp.nikcreations.com/creole-knowledge-portal/auth/callback`
- Dev ALB redirect: `http://ckp-shared-alb-495275305.us-east-1.elb.amazonaws.com/creole-knowledge-portal/auth/callback`

### Deploy fixes applied this session

- Root `Dockerfile` + `.dockerignore` improvements
- `assignPublicIp: true` on Fargate (ECR pull without NAT)
- Separate `webDesiredCount` / `apiDesiredCount` Pulumi config
- TypeScript fixes for Docker build (`DashboardShell`, tabs, `quiz-evaluator`)
- `basePath=/creole-knowledge-portal` baked into production image
