'use client';

/**
 * Previously forced the daily quiz open after 40 minutes of reading.
 * Reading-time auto-open has been removed — this component is intentionally a no-op
 * so existing imports do not break.
 */
export default function ReadingTimerWatchdog() {
  return null;
}
